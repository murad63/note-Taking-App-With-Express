<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ejs</title>
    <link rel="stylesheet" href="/stylesheets/style.css"> 
    <!-- To link any src, no need to mention public folder name. Because we already mentioned it at our index.js file -->
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form">
            <form class="p-10 text-white" action="/create" method="post"> 
                <!-- We need to create to rout at index.js -->
                <input name="title" class="block w-full rounded-lg outline-none  px-5 py-2 bg-zinc-800" type="text" placeholder="Title goes Here">
                <textarea name="details" class="block w-full rounded-lg outline-none  px-5 py-2 bg-zinc-800 resize-none mt-2" placeholder="Write your task details here"></textarea>
                <input class="mt-2 bg-blue-600 px-5 py-2 rounded-md hover:bg-blue-700" type="submit" value="Create Task">
            </form>
        </div>
        <div class="tasks p-10 flex flex-wrap gap-5">
            <% if(files.length > 0) { %> 
                    <% files.forEach(function(file){ %>
                        <div class="task w-1/4 px-3 py-2 rounded-md bg-zinc-800">
                            <h1 class="text-white text-3xl tracking-tighter"><%= file %></h1>
                            <a href="/file/<%= file %>" class="text-blue-500 inline-block mt-2">Read More</a>
                            </div>
                    <% }) %>
                    <%} else { %>
                        <h3 class="text-zinc-600">No Task here</h3>
                    <% } %>
                
            


            
        </div>
    </div>



    <script src="/javascripts/script.js"></script>
</body>
</html>